import path from'node:path';import{fileURLToPath}from'node:url';import express from'express';import{createApp}from'./app.js';const app=createApp(),port=Number(process.env.PORT??8000),dist=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../dist');if(process.env.NODE_ENV==='production'){app.use(express.static(dist));app.get('*splat',(_q,r)=>r.sendFile(path.join(dist,'index.html')))}app.listen(port,'127.0.0.1',()=>console.log(`Application disponible sur http://127.0.0.1:${port}`));
