import Database from 'better-sqlite3';
export function openDatabase(filename='planning.db'){const db=new Database(filename);db.pragma('foreign_keys = ON');db.exec(`CREATE TABLE IF NOT EXISTS clients(id INTEGER PRIMARY KEY AUTOINCREMENT,firstName TEXT NOT NULL,lastName TEXT NOT NULL);CREATE TABLE IF NOT EXISTS employees(id INTEGER PRIMARY KEY AUTOINCREMENT,firstName TEXT NOT NULL,lastName TEXT NOT NULL);CREATE TABLE IF NOT EXISTS interventions(id INTEGER PRIMARY KEY AUTOINCREMENT,clientId INTEGER NOT NULL REFERENCES clients(id) ON DELETE RESTRICT,employeeId INTEGER NOT NULL REFERENCES employees(id) ON DELETE RESTRICT,startAt TEXT NOT NULL,endAt TEXT NOT NULL);`);return db;}
